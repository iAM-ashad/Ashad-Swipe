<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Swipe" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.Swipe.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_bg</item>
        <item name="postSplashScreenTheme">@style/Theme.Swipe</item>

    </style>
</resources>